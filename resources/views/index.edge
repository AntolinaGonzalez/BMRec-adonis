@layout('layouts.admin')

@section('title')
BMRec
@endsection

@section('nav')
<div>
  @if(user)
  <a href="{{ route('admin/HomeController.index')}}" class="navbar-brand">Recommendations</a>
  <a href="{{route('UserController.logout')}}" class="navbar-brand">Logout</a>
  @else
  <a href="/register" class="navbar-brand">Register</a>
  <a href="/login" class="navbar-brand">Login</a>
  @endif

</div>


@endsection
@section('main')
<div class="container center-class">
  <h1>Take a look</h1>
  <p class="paragrafh">You can see the last recommendations. Enjoy!</p>
  <p class="paragrafh">Future actualizations: You will be able to comments and discuss posts</p>
  <div class="row row-cols-3">
    @each(recommendation in recommendations)
    <div class="col">
      <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title">{{recommendation.title}}</h5>
          <p class="card-text">{{recommendation.description}}</p>
          <a href="{{ route('HomeController.show', { id:recommendation.id })}}" class="btn btn-primary btn-style">See</a>
        </div>
      </div>
    </div>
    @endeach
  </div>
</div>
@endsection
